#!/usr/bin/env python3
"""czdev — CardputerZero developer CLI wrapper."""
import os
import sys

# Add scripts/ to path so the czdev package can be found
scripts_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "scripts")
sys.path.insert(0, scripts_dir)

from czdev.__main__ import main

main()
